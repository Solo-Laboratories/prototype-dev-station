package helpers

import (
	"fmt"
	//"os/exec"
)

func DeployGit() {
	// Skeleton code to deploy a Helm chart for Git
	// cmd := exec.Command("helm", "install", "git", "path/to/git/helm/chart")
	// output, err := cmd.CombinedOutput()
	// if err != nil {
	//     fmt.Printf("Error deploying Git: %v\n", err)
	//     return
	// }
	// fmt.Printf("Deploy Git output: %s\n", output)

	fmt.Println("Deploying webtop...")
}
