package helpers

import (
	"fmt"
	//"os/exec"
)

func DeployWebtop() {
	// Skeleton code to deploy Kubernetes manifest files
	// cmd := exec.Command("kubectl", "apply", "-f", "path/to/webtop/manifests")
	// output, err := cmd.CombinedOutput()
	// if err != nil {
	//     fmt.Printf("Error deploying Webtop: %v\n", err)
	//     return
	// }
	// fmt.Printf("Deploy Webtop output: %s\n", output)
	fmt.Println("Deploying webtop...")
}
